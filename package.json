{
  "name": "grok-swap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Independent multi-account OAuth, billing observation, and selection for Grok",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/possibilities/grok-swap.git"
  },
  "bin": {
    "grok-swap": "src/cli.ts"
  },
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "check:contract": "../agentstart/scripts/validate-agent-contract.ts ./src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.2"
  }
}
